# Comma-separated list of Nitter instances to try in order.
# Instances can go up/down; the script will fall back automatically.
# For local Docker instance (after running docker compose up):
# NITTER_INSTANCES=http://localhost:8080
NITTER_INSTANCES=https://nitter.net,https://nitter.poast.org,https://nitter.privacydev.net

# Comma-separated keywords for filtering results (case-insensitive)
# KEYWORDS=Build Canada,BC Open Source,Open Source

# Comma-separated domains to match in text or URLs
# DOMAINS=canadaspends.com,example.org

# Comma-separated account handles to exclude (with or without @)
# BLACKLIST_ACCOUNTS=canada_spends,spambot

# Discord webhook URL for posting new tweets
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_url

# Default query if --q isn't provided (Main Keyword Monitor)
# Recommendation: Use specific handles or distinct project names to avoid noise.
DEFAULT_QUERY=@CanadaSpends OR "CanadaSpends"

# The "Main Account" handle to check if people follow "us"
MAIN_ACCOUNT_HANDLE=canada_spends

# Default max tweets if --limit isn't provided
DEFAULT_LIMIT=20

# Request timeout
TIMEOUT_MS=10000

# How many times to retry per instance (0 or 1 is usually enough)
RETRIES_PER_INSTANCE=1

# twitterapi.io API Key
TWITTER_API_IO_KEY=your_api_key_here

# Path to VIP list JSON file
VIP_LIST_PATH=./vip_list.json


# How often to check for VIP updates (in seconds). Default 3600 (1 hour).
VIP_CHECK_INTERVAL_SECONDS=3600

TRACTION_THRESHOLD_LIKES=5

# How often to check for updates (in seconds). Default 120 (2 minutes).
POLL_INTERVAL_SECONDS=120

# Growth percent to trigger a re-alert on traction (0.5 = 50%)
TRACTION_GROWTH_PERCENT=0.5